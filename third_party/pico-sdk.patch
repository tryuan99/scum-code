--- src/rp2_common/pico_lwip/lwip.BUILD
+++ src/rp2_common/pico_lwip/lwip.BUILD
@@ -90,6 +90,7 @@ cc_library(
 cc_library(
     name = "pico_lwip_http",
     srcs = glob(["src/apps/http/*.c"]),
+    hdrs = ["src/apps/http/fsdata.c"],
     deps = [":pico_lwip_core"],
 )
 
