package(default_visibility = ["//visibility:public"])

filegroup(
    name = "single_channel_calibration_logs",
    srcs = glob(["single_channel_calibration*.csv"]),
)

filegroup(
    name = "multi_channel_calibration_logs",
    srcs = glob(["multi_channel_calibration*.txt"]),
)
